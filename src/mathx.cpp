#include "mathx.h"

// Deliberately misformatted: clang-format wants 2-space indent and no extra
// spacing here. CI must report this WITHOUT rewriting the file, and upload a
// patch instead.
int add( int a,int b )
{
        return a+b;
}

int multiply(int a,int b){
      return a*b;
}
